<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sooying.pay.app.api.dao.datainput.TXDataDao">
	<select id="selectTXDataCountByOrderid" parameterType="java.lang.String"
		resultType="java.lang.Integer">
		select count(*) from dp_sp_payresult_txdatasyn where orderid = #{orderId};
	</select>

	<insert id="addTXData" parameterType="TXDataInfo">
		insert into dp_sp_payresult_txdatasyn
		(spid, orderid, addtime, endtime, userid, price, paytype, status)
		values
		(#{spId},#{orderId},#{addTime},#{endTime},#{userId},#{price},#{payType},#{status});
	</insert>

	<update id="updateTXData" parameterType="TXDataInfo">
		update dp_sp_payresult_txdatasyn
		set spid = #{spId},
		addtime = #{addTime},
		endtime = #{endTime},
		userid = #{userId},
		price = #{price},
		paytype = #{payType},
		status = #{status}
		where orderid = #{orderId}
	</update>
</mapper>